using TBird.Core;

Func<string, string> func = x => x + "+FUNC";

Run(x =>
{
	Console.WriteLine("Console test root.");
	ServiceFactory.MessageService.Info(func(x.Data1));
});

Run(x =>
{
	ServiceFactory.MessageService.Info(func(x.Data2));
});

Ticks.Add(x =>
{
	Info(x.Data1 + "  " + DateTime.Now.ToString("yyyy-MM-dd HH:mm:ss.fff"));
});