<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ResourceDictionary.MergedDictionaries>

        <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Controls.ComboBox.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <Style x:Key="DefaultComboBox" TargetType="ComboBox" BasedOn="{StaticResource MahApps.Styles.ComboBox}">
        <Setter Property="Margin" Value="0" />
        <Setter Property="Padding" Value="3,0" />
        <Setter Property="HorizontalAlignment" Value="Stretch" />
        <Setter Property="DisplayMemberPath" Value="Display" />
    </Style>

    <Style x:Key="DefaultComboBox.Custom" TargetType="ComboBox" BasedOn="{StaticResource DefaultComboBox}">
        <Setter Property="DisplayMemberPath" Value="{x:Null}" />
    </Style>
</ResourceDictionary>