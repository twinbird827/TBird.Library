namespace NewReleaseChecker.App;

// このファイルをコピーして Secrets.cs を作成し、RelayServerApiKey に中継サーバーと同じ共有シークレットを記入する。
// Secrets.cs は .gitignore 済みでコミットされない。
internal sealed class Secrets : NewReleaseChecker.Core.Abstractions.ISecretsProvider
{
    public string RelayServerApiKey => "REPLACE_WITH_RELAY_SHARED_SECRET";
}
