<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:LanobeReader.ViewModels"
             xmlns:converters="clr-namespace:LanobeReader.Converters"
             x:Class="LanobeReader.Views.NovelListPage"
             x:DataType="vm:NovelListViewModel"
             Title="ラノベリーダ">

	<ContentPage.Resources>
		<converters:BoolToVisibilityConverter x:Key="BoolToVis" />
		<converters:InverseBoolConverter x:Key="InverseBool" />
		<converters:IntToBoolConverter x:Key="IntToBool" />
	</ContentPage.Resources>

	<ContentPage.ToolbarItems>
		<ToolbarItem Text="更新" Command="{Binding RefreshCommand}"
                     IsEnabled="{Binding HasCheckError}" />
	</ContentPage.ToolbarItems>

	<Grid>
		<!-- Loading indicator -->
		<ActivityIndicator IsRunning="{Binding IsLoading}" IsVisible="{Binding IsLoading}"
                           HorizontalOptions="Center" VerticalOptions="Center" />

		<!-- Empty state -->
		<VerticalStackLayout IsVisible="False" x:Name="EmptyView"
                             HorizontalOptions="Center" VerticalOptions="Center" Spacing="16">
			<Label Text="タイトルを登録してください" FontSize="18" HorizontalTextAlignment="Center" />
			<Button Text="検索画面へ" Clicked="OnGoToSearchClicked" />
		</VerticalStackLayout>

		<!-- Novel list -->
		<CollectionView ItemsSource="{Binding Novels}" SelectionMode="None">
			<CollectionView.ItemTemplate>
				<DataTemplate x:DataType="vm:NovelCardViewModel">
					<Frame Margin="8,4" Padding="12" HasShadow="True" CornerRadius="8">
						<Frame.GestureRecognizers>
							<TapGestureRecognizer
                                Command="{Binding Source={RelativeSource AncestorType={x:Type vm:NovelListViewModel}}, Path=NavigateToDetailCommand, x:DataType=vm:NovelListViewModel}"
                                CommandParameter="{Binding}" />
						</Frame.GestureRecognizers>

						<Grid ColumnDefinitions="*,Auto" RowDefinitions="Auto,Auto,Auto">
							<!-- Title -->
							<Label Grid.Row="0" Grid.Column="0" Text="{Binding Title}"
                                   FontSize="16" FontAttributes="Bold" LineBreakMode="TailTruncation" />

							<!-- Site badge -->
							<Frame Grid.Row="0" Grid.Column="1" Padding="4,2"
                                   CornerRadius="4" BackgroundColor="#6200EE"
                                   HasShadow="False">
								<Label Text="{Binding SiteTypeLabel}" FontSize="11" TextColor="White" />
							</Frame>

							<!-- Author + Status -->
							<HorizontalStackLayout Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Spacing="8">
								<Label Text="{Binding Author}" FontSize="12" TextColor="Gray" />
								<Label Text="完結" FontSize="11" TextColor="Green"
                                       IsVisible="{Binding IsCompleted}" />
								<Label Text="連載中" FontSize="11" TextColor="Orange"
                                       IsVisible="{Binding IsCompleted, Converter={StaticResource InverseBool}}" />
							</HorizontalStackLayout>

							<!-- Unread + Updated -->
							<HorizontalStackLayout Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Spacing="12">
								<Frame Padding="6,2" CornerRadius="10" BackgroundColor="#FF5722"
                                       HasShadow="False"
                                       IsVisible="{Binding UnreadCount, Converter={StaticResource IntToBool}}">
									<Label Text="{Binding UnreadCount, StringFormat='{0}話未読'}"
                                           FontSize="11" TextColor="White" />
								</Frame>
								<Label Text="{Binding LastUpdatedAt}" FontSize="11" TextColor="Gray"
                                       VerticalOptions="Center" />
							</HorizontalStackLayout>
						</Grid>

						<FlyoutBase.ContextFlyout>
							<MenuFlyout>
								<MenuFlyoutItem Text="キャッシュを削除"
                                    Command="{Binding Source={RelativeSource AncestorType={x:Type vm:NovelListViewModel}}, Path=DeleteCacheCommand, x:DataType=vm:NovelListViewModel}"
                                    CommandParameter="{Binding}" />
								<MenuFlyoutItem Text="タイトルを削除"
                                    Command="{Binding Source={RelativeSource AncestorType={x:Type vm:NovelListViewModel}}, Path=DeleteNovelCommand, x:DataType=vm:NovelListViewModel}"
                                    CommandParameter="{Binding}" />
							</MenuFlyout>
						</FlyoutBase.ContextFlyout>
					</Frame>
				</DataTemplate>
			</CollectionView.ItemTemplate>

			<CollectionView.EmptyView>
				<VerticalStackLayout HorizontalOptions="Center" VerticalOptions="Center" Spacing="16" Padding="20">
					<Label Text="タイトルを登録してください" FontSize="18" HorizontalTextAlignment="Center" />
				</VerticalStackLayout>
			</CollectionView.EmptyView>
		</CollectionView>
	</Grid>
</ContentPage>